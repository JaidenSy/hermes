name: CI

on:
  push:
    branches: ["main", "feature/**"]
  pull_request:
    branches: ["main"]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install pytest pyyaml requests keyring watchdog

      - name: Symlink repo to ~/engram (tests use Path.home() / "engram")
        run: ln -sf "$GITHUB_WORKSPACE" "$HOME/engram"

      - name: Run tests
        run: pytest tests/ --ignore=tests/test_engram_endpoints.py -v
        working-directory: ${{ github.workspace }}
